namespace $.$$ {
	const { per } = $mol_style_unit;
	$mol_style_define($novorostorgi_header, {
		background: { color: "white" },
		Row: {
			alignItems: "center",
			// justifyContent: "space-between",
			width: per(100),
		},
		// Col_logo: { width: per(20) },
		// Col_nav: { width: per(40) },
		// Col_button: { width: per(30) },
		// Body: { background: { color: "white" } },
	});
}
